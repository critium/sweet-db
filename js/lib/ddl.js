"use strict"

var fs = require('fs');

this.create = function create(rootFile, custFile) {
  console.log(custFile);
  //var custJSON = JSON.parse(custFile);
  // look for tables in the customer file
  // compare with the abstract columns
};
